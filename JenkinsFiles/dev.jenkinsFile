pipeline {
   agent any

   stages {
      stage('pull code') {
         steps {
            git branch: 'master',credentialsId: 'github.com',url: 'git@github.com:moomdate/SUT-REG-API.git'
         }
      }
   }
}
